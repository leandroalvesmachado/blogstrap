<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <!--
      <a href="<%= article_path(article) %>">
        <%= article.title %>
      </a>
      -->

      <!-- Equivalente ao a href -->
      <%= link_to article.title, article_path(article) %>
    </li>
  <% end %>
</ul>
